spring:
  cloud:
    consul:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9999

erpnext:
  domain: test.local
  api-key: ${ERPNEXT_API_KEY:}
  api-secret: ${ERPNEXT_API_SECRET:}
